#include "main.h"

/**
 * Main : a function that prints an order of string in reverse
 *
 * Return : Akways 0
 */
void rev_string(char *s); 
{
	int len = strlen(*s);

	while (len--)
	{
		_putchar(*(s + len);
		_putchar(10);
	}


}
