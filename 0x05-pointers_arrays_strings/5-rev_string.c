#include "main.h"
#include <stdio.h>

/**
 * Main : a function that prints an order of string in reverse
 *
 * Return : Akways 0
 */
void rev_string(char *s); 
{
	int len = strlen(*s);

	while (len--)
	{
		putchar(*(s + len);
		putchar(10);
	}


}
