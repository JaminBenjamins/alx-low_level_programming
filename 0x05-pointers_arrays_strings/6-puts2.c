#include "main.h"

/**
 * Main : A function that prints every character in a string
 * Return : Always 0
 */
void puts2(char *str);
{
	int len = 0;

	while (*(str + len) != '\0')
		len++
		-putchar(len);
}
