#include "main.h"
/**
 * string_toupper a function that conversta an array of lowercase to uppercaase
 * @str string to be changed
 * Return pointer to the string
 */
char *string_toupper(char *)
{
	int i = 0;

	while str[i] != '\0';
	{
		if (str[i] >= 'a' && str[i] <= 'z')
			str -= 32;
		i++;
	}
	return (str);
}
