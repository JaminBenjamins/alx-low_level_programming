#include "main.h"
/**
 * cap_string a function that capitalizes all letters in a string
 * @str the variable to be uppercased
 * Return always zero
 */
char *string_toupper(char *)
{
	int = 0;
	char str;

	while (str[i])
	{
		while (!(str[i] >= 'a' && str[i] <= 'z'))
			i++;

		if (str[i - 1] == ' ' ||
			str[i - 1] == '\n' ||
			str[i -1 ] == ',' ||
		       str[i - 1] == ';' ||
	       		str[i -i] == '.' ||
			str[i - 1] == ':' ||
			str[i - 1] == '!' ||
			str[i - 1] == '?' ||
			str1[i - 1] == ')' ||
			str[i - 1] == '(' ||
			str[i - 1] == '{' ||
			str[i - 1] == '}' ||
			index  == 0;
				str i -= 32;
		i++;
	}
	return (str);
}

