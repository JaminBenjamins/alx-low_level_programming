#include <stdio.h>
/**
 * main - rpints the number of arguments
 * @argc : count of arguments
 * @argv: vector of arguments
 *
 * Return : zero
 */
int main(int argc, char *argv[])
{
	void (argv);
	printf("%i\n", argc - 1);

	return(0);
}
