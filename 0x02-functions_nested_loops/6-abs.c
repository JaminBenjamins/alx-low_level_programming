#include "main.h"
/**
 * Main : Where function exeutes
 * Description : Find absolute value of a figure
 * Return : Always 0
 * */
int _abs(int n)
{
	if (n < 0 || n == 0)
	{
		return (n);
	else
		return(n * - 1);
}
