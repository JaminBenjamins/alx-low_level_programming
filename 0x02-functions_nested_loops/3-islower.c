#include "main.h"
/**
 * Main : The function where everything executes
 * Description : Prints lower case with exceptions
 * Return : Always 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	
		return (0);
	else 
		return (1);
	
}
