#include <stdio.h>

/** This is a programm that prints alphabet in lowercase
 * The program runs in the main function
 * */

int main(void)
{
	int a;
	for (a = 'a'; a < 'z'; a++)
		putchar(a);
	putchar ('\n');
	return (0);
}

