#include <stdio.h>

/** 
 * Main : This is a programm that prints alphabet in lowercase
 * Return : Always 0
 */ 

int main(void)
{
	int a;
	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar ('\n');
	return (0);
}

