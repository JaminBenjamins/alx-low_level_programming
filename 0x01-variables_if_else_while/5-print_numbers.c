#include <stdio.h>
/**
 * Main - function where everything is called
 *
 * Return : 0
 */

int main(void)
{
	int ch;

	{
	for (ch = 0; ch < 10; ch++)
	printf("%d", ch);
	}
	printf("\n");

	return (0);
}
